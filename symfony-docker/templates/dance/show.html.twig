{% extends 'base.html.twig' %}

{% block title %}Dance{% endblock %}

{% block body %}
    <h1>Dance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dance.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dance.name }}</td>
            </tr>
            <tr>
                <th>Tact</th>
                <td>{{ dance.tact }}</td>
            </tr>
            <tr>
                <th>Difficulty</th>
                <td>{{ dance.difficulty }}</td>
            </tr>
            <tr>
                <th>DefaultBPM</th>
                <td>{{ dance.defaultBPM }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dance_index') }}">back to list</a>

    <a href="{{ path('app_dance_edit', {'id': dance.id}) }}">edit</a>

    {{ include('dance/_delete_form.html.twig') }}
{% endblock %}
